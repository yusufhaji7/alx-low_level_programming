#include <stdio.h>

/**
 * main - function to start any c program
 * Return: 0 value
 */
int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art,");
        return (0);
}
